name: xmpp_dart
description: A lightweight Dart port of xmpp.js — TCP XMPP client.
version: 0.1.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.12.2

dependencies:
  crypto: ^3.0.0
  xml: ^7.0.1

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
